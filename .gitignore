# ---------- 依赖 ----------
node_modules
server/node_modules/
server/.env

# ---------- 构建产物 ----------
dist
dist-ssr

# ---------- 本地缓存与临时文件 ----------
.npm-cache
.vite
*.local
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ---------- 编辑器 / 系统 ----------
.DS_Store
Thumbs.db
.idea
.vscode/*
!.vscode/extensions.json

# ---------- 验证脚本的运行时产物 ----------
# 脚本本身（_verify/scripts/）保留在库里，方便日后复查；
# 下面这些是运行它们时产生的：浏览器临时 profile、截图、量测报告。
_verify/ws-*
_verify/shots
_verify/*.json
_verify/*.png
_verify/*.log
